import {ReactElement} from "react";

const plusFunction = (cisloA: number, cisloB: number): number => {
    return cisloA + cisloB;
}


export default function NavBar (): ReactElement {
    return (
      <div>
          wow such nav
      </div>
    );
}